#ifndef TYPES_EGG_H
#define TYPES_EGG_H
#include <types.h>

namespace EGG
{
    template <typename T> struct Math;
    struct Matrix34f;
    struct Quatf;
    struct Vector3f;

    struct Heap;

    struct Disposer;

    struct Allocator;

    struct Xfb;
    struct XfbManager;

    struct Archive;

    struct MsgRes;
}

#endif